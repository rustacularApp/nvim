# Installation for typst

1. Ensure Cargo is installed
2. install typst-cli using 
```
cargo install --locked typst-cli
```
3. install tinymist using
```
cargo install --locked tinymist
```
4. fork the nvim project
5. use tool
